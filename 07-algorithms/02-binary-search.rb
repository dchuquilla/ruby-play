require './01-quicksort.rb'

def binarySearch(arr, needle, init, last)
  m = (init + last)/2
  if arr[
end

arr = [1, 5, 2, 8, 4, 3, 9, 0, 7, 6]
quickSort(arr, 0, arr.length-1)

binarySearch(arr, 0, 0, arr.length-1)
